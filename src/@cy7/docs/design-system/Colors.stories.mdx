import { palette } from "@cy7/designSystem/primitives/palette";
import { ColorSwatch, SwatchGroup } from "@cy7/docs/utils";
import { Meta, Preview, Story } from "@storybook/addon-docs/blocks";

<Meta title="Primitives / Colors" />

# Colors

TODO: update docs since refactoring to usage-based names (like "bg" and "bodyText") to enable color modes.

<Preview>
  <Story name="Color palette">
    <SwatchGroup>
      {Object.keys(palette).map(color => (
        <ColorSwatch color={palette[color]} name={color} />
      ))}
    </SwatchGroup>
  </Story>
</Preview>
