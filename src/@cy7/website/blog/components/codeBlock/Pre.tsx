import { styled } from "@cy7/design-system";

const Pre = styled("pre", {
  color: "$codePlain",
  display: "inline-block",
  hyphens: "none",
  padding: "$1",
  margin: "$0",
  minWidth: "100%",
});

export default Pre;
